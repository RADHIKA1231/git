Steps to link lr to gr
>git init (.git)--local repository ##git initialsing
>git config 
>git remote add origin "github url" ##local repository->global repository
>git add -A ##to add up all the files at a time
>git status
>git commit -m"1st version"  ##m-message
>git push origin master